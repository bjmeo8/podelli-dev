# Podelli Frontend

## Setup

1. Install Firebase CLI: `npm install -g firebase-tools`
2. Login: `firebase login`
3. Update Firebase config in `assets/js/auth.js`
4. Add Firebase SDK to HTML pages

## Deploy

```bash
firebase serve     # Test locally
firebase deploy    # Deploy to hosting
```

## Next Steps

1. Create Firebase project "podelli-dev"
2. Enable Authentication
3. Get Firebase config
4. Update auth.js with config
5. Deploy

See DEPLOYMENT_PHASE1.md for detailed instructions.
